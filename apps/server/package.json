{
  "name": "@tavern-forge/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx --watch src/index.ts",
    "start": "node --env-file=../../.env --import tsx src/index.ts",
    "test": "node --env-file=../../.env --import tsx --test src/config/index.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@tavern-forge/contracts": "workspace:*",
    "fastify": "^5.3.0"
  }
}
